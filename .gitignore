# dependencies / build
node_modules/
.netlify/
_site/

# secrets / local env — never commit
.env
.env.*

# local working notes — NEVER publish (owner name, project IDs, handoff)
CLAUDE.md
.remember/
NOTES*.md
HANDOFF*.md
*AUDIT*.md

# import artifacts
extracted/
*.zip

# raw audio sources — the SFX/anthems ship embedded as base64 in index.html, not as files
sounds/
deno.lock

# private research notes (owner's forensic reports) — never publish
deep research/
*research*.md

# internal design references / screenshots (not for the public repo)
player-tags-selection.png
ChatGPT Image*.png
Image 1*.png
cards/

# os
.DS_Store
Thumbs.db
